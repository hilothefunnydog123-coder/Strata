name: Deprecated (Ward Desktop)

# The Ward Desktop (Python) build has been retired. Ward is now a Next.js
# web application; see .github/workflows/ci.yml for the active pipeline.
# This workflow is manual-only and intentionally does nothing.

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Ward Desktop has been retired. See ci.yml for the web app pipeline."
