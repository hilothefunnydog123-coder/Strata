name: Deprecated (Strata Desktop)

# The Strata Desktop (Python) build has been retired. Strata is now a Next.js
# web application; see .github/workflows/ci.yml for the active pipeline.
# This workflow is manual-only and intentionally does nothing.

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Strata Desktop has been retired. See ci.yml for the web app pipeline."
