# Dependencies
node_modules/
.pnpm-store/
.pnp
.pnp.js

# Build output
dist/
.next/
out/
build/
.turbo/
*.tsbuildinfo
next-env.d.ts

# Tauri / Rust
apps/desktop/src-tauri/target/
target/

# Local data & secrets
.env
.env.local
.env.*.local
*.local.db
*.sqlite
*.sqlite-*
/data/

# Pipeline runtime scratch. NOTE: /fixtures (committed raw snapshots) and the
# fixture LLM cache ARE tracked — offline dev depends on them. Only the live
# runtime raw store and live LLM cache are ignored.
/.assent-raw/
packages/extract/.llm-cache/live/

# Logs & OS
*.log
npm-debug.log*
.DS_Store
Thumbs.db
*.pem

# Editor
.vscode/
.idea/
